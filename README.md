# Clicker Wars Game — Практический проект по веб-разработке

## Обзор проекта
Создайте игру-симулятор избирательной кампании, где участники играют за соперничающие партии и соревнуются в "результативности" выборов. Каждый клик — это +1 к результатам вашей партии, но будьте осторожны: сроки работы комиссии ограничены!

## Технические требования
- React 18+ с TypeScript
- Инструмент сборки на выбор (рекомендуется Vite)
- Обязательное использование строгого режима TypeScript

## Начало работы: "Открытие избирательного участка"

### Шаг 1: Создание своей ветки власти
Каждый участник проекта должен создать свою собственную ветку разработки:
```bash
# Посмотрите на текущий коммит (ваша отправная точка)
git log --oneline -5

# Создайте ветку от текущего коммита с вашим именем
git checkout -b ваш-никнейм-ветка
```

### Шаг 2: Установка "избирательных бюллетеней"
Установите все необходимые пакеты для работы:
```bash
npm install
```

### Шаг 3: Локальная проверка системы
Запустите проект локально для тестирования:
```bash
npm run dev
```

После этого проект будет доступен по адресу `http://localhost:5173` (или другому порту, в зависимости от вашей настройки).

### Шаг 4: Деплой на "центральный сервер"
После завершения работы над этапом и проверки локально, закоммитьте изменения и отправьте ветку в репозиторий:

```bash
# Добавьте все изменения
git add .

# Создайте коммит с описанием выполненной работы
git commit -m "Завершил этап X"

# Отправьте ветку на сервер
git push origin <ветка>
```

### Магия автоматического деплоя 🪄
**Внимание!** При пуше в вашу ветку автоматически запускается процесс деплоя:

1. GitHub Actions собирает ваше приложение
2. Вам автоматически назначается домен третьего уровня

**Ваш сайт будет доступен по адресу:**
```
https://<название-вашей-ветки>.clicker-wars.visoff.ru
```

*Примечание: Домен станет доступен через 1-2 минуты после пуша. Если что-то пошло не так — проверьте вкладку "Actions" в репозитории.*

### Шаг 5: Проверка "явки"
После деплоя обязательно проверьте:
1. Открывается ли ваш сайт по назначенному домену
2. Работают ли все функции текущего этапа
3. Нет ли ошибок в консоли браузера

## Структура этапов

### Этап 1: Базовый избирательный участок
Создайте локальные "выборы" без сохранения протоколов и контроля времени.
- Две кнопки для "Партии Порядка" и "Партии Перемен"
- Счётчик "явки" в реальном времени для обеих партий
- Кнопка "Открыть участок" для начала голосования
- Кнопка "Завершить досрочно" для экстренного закрытия участка
- Автоматический подсчёт "результатов" по количеству кликов
- Протоколы не сохраняются (всё "теряется" при обновлении страницы)

### Этап 2: Работа комиссии по графику
Добавьте ограниченное время работы избирательной комиссии.
- Стандартный срок подсчёта — 30 секунд
- Таймер обратного отсчёта "Часы комиссии"
- Автоматическое завершение по истечении сроков
- Возможность "ускорить подсчёт" (досрочное завершение)
- Сброс таймера для новых "выборов"
- Блокировка "голосования" после закрытия участка

### Этап 3: Архив избирательных протоколов
Реализуйте сохранение "истории выборов" в локальное хранилище.
- Сохранение итогов голосования в localStorage
- Протоколы сохраняются даже при "внезапных проверках" (обновлении страницы)
- Архив последних 10 избирательных кампаний
- Функция "Уничтожить компрометирующие материалы" (очистка истории)
- Полный сброс данных с подтверждением

### Этап 4: Центральная избирательная система
Создайте систему управления несколькими "избирательными участками".
- Форма создания участка (названия партий, длительность голосования)
- Список активных участков со статусами
- Возможность присоединиться к любому участку в качестве "активиста" любой партии
- Просмотр хода голосования с обновлениями в реальном времени
- Управление несколькими участками в локальном "архиве"
- Поиск и фильтрация участков

### Этап 5: Общегосударственная система
Подключение к центральному серверу Единой Государственной Системы (спецификация будет предоставлена).

**Структура API (самая простая):**

**Базовый URL:** `https://api.clicker-wars.visoff.ru/`

**Типы данных:**
```typescript
interface Election {
  id: string;
  party1Name: string;
  party2Name: string;
  team1Score: number;
  team2Score: number;
  duration: number; // в секундах
  status: 'created' | 'active' | 'finished';
}

interface CreateElectionRequest {
  team1Name: string;
  team2Name: string;
  duration: number;
}

interface VoteRequest {
  election_id: string;
  party: 1 | 2;
}
```

**Эндпоинты:**

1. `GET /elections` - получить список всех выборов
   - Ответ: `Election[]`

2. `POST /elections` - создать новые выборы
   - Тело: `CreateElectionRequest`
   - Ответ: `Election`

3. `GET /elections/:id` - получить информацию о конкретных выборах
   - Ответ: `Election`

4. `POST /elections/:id/vote` - проголосовать (кликнуть) за партию
   - Тело: `VoteRequest`
   - Ответ: `{ success: boolean, currentScores: [number, number] /* team1Score, team2Score */ }`

**Примечания к реализации:**
- Голосование разрешено только если `status === 'active'`
- Для реального обновления счетчиков можно использовать периодический опрос (polling) или слушать обновления через Server-Sent Events (SSE) на `POST /elections/:id/vote/sse`

## Ограничения и требования
- Обязательное использование React с TypeScript
- Запрещено использование "искусственного избирателя" (LLM)
- Строгая типизация TypeScript (во избежание неоднозначностей)
- Компонентная архитектура (чтобы всё было прозрачно)
- Чёткое разделение ответственности (как в хорошей комиссии)

## Самопроверка
Каждый самостоятельно проверяют, что их приложение:
- Запускается без "чрезвычайных происшествий"
- Работает согласно утверждённому регламенту (требованиям этапа)
- Корректно обрабатывает "спорные ситуации" (граничные случаи)
- Сохраняет "целостность данных" там, где это предусмотрено

## Важные замечания
1. **Работа в своей ветке**: Не коммитьте и не пушите в main ветку
2. **Регулярные коммиты**: Делайте коммиты после завершения каждой логической части
3. **Проверка деплоя**: Всегда проверяйте, что ваш сайт работает после пуша
4. **Время деплоя**: Первый деплой может занять 3-5 минут, последующие — 1-2 минуты
5. **Логи ошибок**: Если что-то сломалось, проверьте вкладку Actions → ваш последний пуш

## Техническая поддержка
При возникновении проблем с деплоем или доступом к вашему сайту:
1. Проверьте статус в Actions в репозитории
2. Убедитесь, что сборка проекта проходит локально без ошибок
3. Если проблема сохраняется — обратитесь к "главному системному администратору" (преподавателю)

## Дополнительные идеи
- Система "карусели" (ограничение скорости кликов)
- Визуальные эффекты "чудо-сканера" бюллетеней
- Звуки печатающего календаря и шуршания бюллетеней
- Настраиваемые цвета партий (в рамках утверждённого регламента)
- "Особые полномочия" (усилители кликов)
- Статистика явки и эффективности по участкам
- Система "дружественных наблюдателей"
